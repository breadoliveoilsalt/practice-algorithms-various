/*

- Rem with priority queue, you keep bubbling up until parent's priority is >= child's priority.  That way, if two nodes have the same priority, the one that was inserted first is the first to go.
*/
